{
    "version": "0.2.0",
    "configurations": [
        {
           "terminal":"",
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./a.out",
            "cwd": "${workspaceRoot}"
        }
    ]
}